node_modules
dist
dev-dist
.env
.env.local
.vercel
*.log
.DS_Store
.superpowers/
.worktrees/
supabase/.temp

# Local scratch copies — keep them on disk, never in the repo
*.backup

# Local-only backups, never committed
local-backups/
.claude/
.env*
