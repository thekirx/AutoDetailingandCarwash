# Principal QA run log — 2026-08-13

Continuation of the matrix plan: remaining live **write** flows + P1-1 reviews RLS.

## Done means (this pass)

1. P1-1 `service_reviews` is no longer `using (true)` / `with check (true)`.
2. C3 Failed QA, C5 POS sale/expense/CA, C8 planner proof, C9 customer review run against live `lybxhpzzqqyqswvuwpxv` with RLS.
3. Automated gates exit 0.

## Defects found and closed

| ID | Severity | Issue | Fix |
|---|---|---|---|
| P1-1 | P1 | `service_reviews` insert/select `true` for all authenticated | Migration `20260813120000_service_reviews_rls.sql` — customer owns completed booking; ops SA/ASA/BA select |
| P1-4 | P1 | Staff proof `in_progress → for_review` blocked by `guard_plan_card_assignee_self_update` | Migration `20260813121000_planner_proof_transitions.sql` |

## Evidence

| Gate | Exit | Result |
|---|---|---|
| `node --test` (plan suite + matrix/flows + RLS/proof) | **0** | **137 pass / 0 fail** |
| `node scripts/_qa-live-smoke.mjs` | **0** | **18/18** |
| `node scripts/_qa-live-writes.mjs` | **0** | **16/16 PASS** |
| `npm run build` | **0** | Vite production ~50s |
| Live policies | — | insert_own + select_ops (not `true`) |

### Live writes (`scripts/_qa-live-writes.mjs`)

| ID | Result |
|---|---|
| C3-ticket → in_progress → final_checking → **Failed QA (redo)** → for_payment | PASS (TL client RLS) |
| C5-pos-sale (`complete_pos_sale` cash) | PASS |
| C5-expense `expense_kind=daily` | PASS |
| C5-ca-approve (`ops_form_submissions` new → resolved) | PASS |
| C8-assign + proof `for_review` + reviewer done | PASS |
| C9-staff-denied (42501) | PASS |
| C9-customer-review | PASS |
| C9-investor-hidden | PASS |
| C9-admin-read | PASS |

QA sale/expense/review/card rows cleaned after the run. SMS toggle forced off during writes.

## P1-2 / P1-3 closed (2026-08-13 follow-up)

| ID | Issue | Fix |
|---|---|---|
| P1-2 | Settings/crew read-write `rules` json; live table is scalar columns — SA save no-op | `normalizeCompensationSettings` + `toCompensationSettingsRow` |
| P1-3 | CRM line items `.in(ids.slice(0, 200))` | `chunkIds` + loop in `CrmInsightsPanel` |

Estimate remains estimate (no payroll ledger). Settings now persist and crew estimate uses saved %.

CRM directory + insights no longer silently cap at 200 / 2000 rows (`collectPaged` + `chunkIds`). Vehicle-photos bucket + storage policies exist on live.

P0-1/P0-2 remain closed from `3b40fab`.

## P1-5 / C10 close (2026-08-13 follow-up 2)

| ID | Issue | Fix |
|---|---|---|
| P1-5 | Floor/KPI/Reports/Finance/compensation still silently capped (2000/800/500/300 / PostgREST 1000) | `collectPaged` + `collectInChunks` on remaining money/ops queries |
| C10 | Compensation estimate had no Finance write | `buildDailyCompensationExpense` → draft `salary_carwash` expense, once per branch per day |

Stale nav/status contract tests aligned to shipped dual-chrome + detailing pipeline (`for_releasing`, `canMarkFailedQa`).

### Evidence (this session)

| Gate | Exit | Result |
|---|---|---|
| `node --test` | **0** | **580 pass / 0 fail** |
| `npm run build` | **0** | Vite production 40.65s |
| `node scripts/_qa-live-smoke.mjs` | **0** | **18/18** |
| `node scripts/_qa-live-writes.mjs` | **0** | **17/17** including **C10-comp-post** (BA RLS insert `salary_carwash`, cleaned) |

Known non-blockers: compensation posts a Finance **draft** (SA/admin only; TL is RLS-denied on `expenses` insert). Advisors still flag existing SECURITY DEFINER helpers/views. Brand Assets remain untracked.

## Remaining close plan (this pass)

Done means: leftover review gaps closed, TDD on the public seams, gates green, `main` pushed.

| Slice | Seam | Test | Implementation |
|---|---|---|---|
| 1 | `buildCompensationPostPlan` | per-branch pool, late weight 0.7, skip posted | Crew tab uses helper (no flatten-to-present) |
| 2 | Unique `compensation:*` expense | live 2nd insert fails 23505 | partial unique index on `expenses.description` |
| 3 | POS today sales + TL day pesos | source no `.limit(500)` / `.limit(300)` | `collectPaged` |
| 4 | Finance purchases see kind | select includes `expense_kind` | add column to paged select |
| 5 | CRM branch directory | no `.limit(2500)` | `collectPaged` on booking customer_ids |

Out of scope: Brand Assets binaries, rewriting SECURITY DEFINER advisors, BookingBoard/planner list windows, auto payroll ledger (Finance still approves drafts).

### Evidence (remaining close)

| Gate | Exit | Result |
|---|---|---|
| `node --test` | **0** | **581 pass / 0 fail** |
| `npm run build` | **0** | Vite 21.02s |
| `node scripts/_qa-live-writes.mjs` | **0** | **18/18** including **C10-comp-unique 23505** |
| Live index | — | `expenses_compensation_day_uidx` on `lybxhpzzqqyqswvuwpxv` |

## Postgres + paging follow-up

| Fix | Why |
|---|---|
| `sales_paid_branch_occurred_idx` partial `(branch, occurred_at desc) where status = 'paid'` | Hot floor/POS/CRM/compensation filter |
| Drop `idx_bookings_vehicle_plate`, `sales_customer_occurred_idx` | Advisor duplicate indexes |
| `collectInChunks` + `.order(id)` on CRM/Reports line items; staff assignments ordered | Unstable `.range()` without ORDER BY |
| Floor started-jobs `.order(in_progress_at)` | Same skip/dup risk |

### Evidence

| Gate | Exit | Result |
|---|---|---|
| `node --test` | **0** | **581 pass / 0 fail** |
| `npm run build` | **0** | Vite 18.12s |
| `node scripts/_qa-live-writes.mjs` | **0** | **18/18** |
