# Settings

**Routes:** `/operations/settings`, `/settings/pos`, `/settings/payroll`  
**Shell:** Command

## Purpose
Hub tiles → POS policy, payroll policy.

## Components
PageHeader, card tiles (interactive), forms.
