node_modules
dist
dev-dist
.env
.env.local
.vercel
*.log
.DS_Store
.superpowers/
.worktrees/
supabase/.temp

# Local scratch copies — keep them on disk, never in the repo
*.backup

# Local-only backups, never committed
local-backups/
.claude/
.env*

# Raw camera masters for the proof clips. The web-ready AV1/H.264 encodes and
# their posters are committed; the originals live in Google Drive, because
# 164MB of masters is weight the repository would carry forever.
public/video/service-proof/*/source/
