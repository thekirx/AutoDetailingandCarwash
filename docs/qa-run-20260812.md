# Principal QA run log — 2026-08-12

Target: Hakum dual-chrome redesign + role matrix  
App DB: `https://lybxhpzzqqyqswvuwpxv.supabase.co`  
Mode: automated gates + permission matrix + live read smoke (demo auth)

## Phase A — Automated gates

| Command | Exit | Result |
|---|---|---|
| `node --test` (plan suite + `principalQaMatrix` + `principalQaFlows`) | **0** | **133 pass / 0 fail** |
| `npm run build` | **0** | Vite production build OK (~25s) |

Plan suite files: `hakumRedesignLibs`, `floorBoardLanes`, `superAdminFloor`, `appShell`, `prodShellContract`, `branchAdminShell`, `permissions`, `permissions.marketingSales`, `authRedirect`, `salesRole`, `clientOpsFixes`, `commandCategories`, `tlFloorDetailing`, `teamLeadQueueManager`, `queueLogic`, `posSale`, `posBranchScope`, plus new principal QA tests.

## Phase E / P0 fixes (closed)

| ID | Issue | Fix | Verify |
|---|---|---|---|
| **P0-1** | Detailer home/dock → queue but `allowRoute('queue')` denied | Added `DETAILER` to `QUEUE_VIEWER_ROLES` | Matrix test + break/restore regression (`regressionCaught:true`, `restoredPass:true`) |
| **P0-2** | Video editor dock → my-tasks but `canViewAssignedTasks` denied | Included `VIDEO_EDITOR` in `canViewAssignedTasks` | Same |

## Phase B — Role × home × shell (automated)

Evidence: `tests/principalQaMatrix.test.js` (all PASS).

| Role | Home | Shell |
|---|---|---|
| BossMich | `/operations/console` | Command |
| ASA | `/operations/console` | Command |
| admin | `/operations/pos` | Command |
| investor | `/operations/finance` | Command |
| team_lead | `/operations/queue` | FloorApp |
| staff | `/operations/attendance` | FloorApp |
| sales | `/operations/bookings` | FloorApp |
| marketing | `/operations/crm` | FloorApp |
| video_editor | `/operations/planning?tab=calendar` | FloorApp |
| detailer | `/operations/queue?family=detailing` | FloorApp |
| customer | `/account` (via `resolveAppHome`) | CustomerAppFrame |

Negative denials covered: staff→finance/pos/people; sales→pos/finance/queue; investor→finance+reports only; TL→pos/finance denied.

## Phase C — Flows

### Automated / structural (`principalQaFlows.test.js`) — PASS

C1 provision roles in `provisionStaff.mjs`; C3 Failed QA gates; C4 detailing pipeline IDs; C5 POS tabs + payment methods + Bacoor report; C8 planner proof wiring; C9 CRM CSV + reviews access; C10 compensation helpers; C11 BA/ASA/investor scope.

### Live Supabase smoke (`node scripts/_qa-live-smoke.mjs`) — **18/18 PASS**, exit 0

| ID | Result |
|---|---|
| C1-roles | Live roles include BossMich, ASA, admin, TL, sales, marketing, staff, **detailer, video_editor, investor** |
| C1-provision-gap | **CLOSED** — seeded via `scripts/seed-floor-accounts.mjs` + demo chips |
| C3-bookings | Readable; sample statuses completed\|waiting\|cancelled |
| C5-sales / expenses | Readable |
| C7 staff_attendance | Readable |
| C8 plan_cards | Readable |
| C9 service_reviews | Readable |
| C10 compensation_settings | Readable |
| login-* | TL/sales/admin/marketing/staff/**detailer/video/investor**/customer auth + role match |

**Not executed as full UI click-through in this run:** wash Failed-QA notify, POS checkout payment, CA approve, planner proof submit, customer rating UI. Contracts + live auth for all matrix roles verified.

## Phase D — Chrome / usability

| Check | Evidence | Result |
|---|---|---|
| Dual Command / FloorApp | `OperationsLayout.jsx` + shell tests | PASS |
| Brand navy `#052699` / page `#f1f1ed` | `design-tokens.css`, `docs/ops-chrome.md` | PASS |
| Customer frame | `CustomerAppFrame.jsx` + App routes | PASS |
| FloorApp safe-area / dock | `teamLeadQueueManager` tests | PASS |

## Open non-blockers (from plan)

| ID | Severity | Status |
|---|---|---|
| P1-1 | P1 | `service_reviews` RLS still broad — documented, not blocking matrix |
| P1-2 | P1 | Compensation estimate-only |
| P1-3 | P1 | CRM insights ~200 sale ID cap |
| P2-1 | P2 | **CLOSED** — demo chips + live seed for detailer/video/investor |

## Done criteria

1. Role home/shell/allowRoute matrix — **PASS** (incl. fixed P0s)  
2. Surfaces evidenced via tests + live smoke — **PASS** (UI click-through residual noted)  
3. Critical data access on live DB — **PASS** (read smoke + all-role login)  
4. Automated gates + build — **PASS** exit 0  
5. P0 access bugs — **CLOSED**

**Verdict: COMPLETE for principal QA plan scope** (automated + live auth/data smoke; P0/P2 closed; P1 documented).
